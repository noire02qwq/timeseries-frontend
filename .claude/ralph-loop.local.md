---
active: true
iteration: 1
max_iterations: 5
completion_promise: "DONE"
started_at: "2026-03-15T12:40:11Z"
---

现在是一个前端程序来对接后端的机器学习数据分析程序，但是只是ui的demo，现在需要编写它的接口。这个程序应该运行在110端口上，后端预计运行在310端口上，你需要理解这个前端程序的所有任务，对后端程序发出正确的指令，然后正确接收后端程序的反馈。另外对于训练、调参、测试界面，需要再添加一个浮动弹窗来接受脚本的实时log日志。接口设计完之后，再编写一demo-server的模拟后端js程序，模拟接收前端的指令、生成配置文件，执行训练调参等工作、反馈结等操作，目的是确保前端的接口都可以正常运行。最后要更新全部的md文档，使得接口清晰，后端开发着可以理解这个前端程序的全部功能。
